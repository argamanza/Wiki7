services:
  scraper:
    build:
      context: .
      dockerfile: Dockerfile.scraper
    volumes:
      - ./tmk-scraper/output:/app/tmk-scraper/output
    environment:
      - SCRAPERAPI_KEY=${SCRAPERAPI_KEY:-}
